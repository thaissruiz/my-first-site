window.onload = function() {
    var button = document.querySelector('button');
  
    button.addEventListener('click', function() {
      alert(" I  can be better every day");
    });
  }

  //Botao que mostra mensagem
